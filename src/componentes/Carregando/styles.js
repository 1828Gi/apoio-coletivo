import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imagemCarregando: {
    resizeMode: 'contain',
    alignSelf: 'center',
  },
  textoCarregando: {
    textAlign: 'center',
    fontSize: 20,
    fontWeight: 'bold',
  },
});
