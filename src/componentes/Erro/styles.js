import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerErro: {
    justifyContent: 'center',
    padding: 32,
  },
  textoEmojiErro: {
    marginVertical: 48,
    fontSize: 64,
  },
  textoErro: {
    textAlign: 'center',
    fontSize: 20,
  },
});
